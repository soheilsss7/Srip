# SRIP — معرفی ساده و روان (متن ارائه)

*این نسخه برای ارائه است: بدون اصطلاح فنی، بدون ارجاع به کد. طوری نوشته شده که بتوانید همان را بلند بخوانید یا از رویش توضیح دهید.*

---

## ۱. در یک جمله

**SRIP سامانه‌ای است که «روابط کاری» یک هلدینگ را از ذهن آدم‌ها بیرون می‌کشد، ثبت می‌کند، به آن‌ها نمره می‌دهد، و می‌گوید قدم بعدی چیست.**

---

## ۲. مسئله‌ای که حل می‌کند

هر شرکتی — به‌خصوص هلدینگ‌هایی با چند شرکت زیرمجموعه — سرمایه‌ای دارد که در هیچ ترازنامه‌ای نوشته نشده: **رابطه‌ها**.
رابطه با بانک، با مدیر دولتی، با مشتری کلیدی، با شریک تجاری، با سرمایه‌گذار.

امروز این سرمایه این‌طور مدیریت می‌شود:

- در گوشی و ایمیل و حافظهٔ چند نفرِ خاص.
- آن آدم که همهٔ رابطه‌ها دستش بود می‌رود → رابطه هم می‌رود.
- مدیرعامل می‌پرسد «وضعیت رابطه‌مان با آن بانک چطور است؟» → جوابی عددی وجود ندارد.
- فرصت‌ها از دست می‌روند، چون کسی یادش نبوده شش هفته است با آن طرف صحبت نشده.
- هیچ‌کس نمی‌داند بین شرکت ما و آن سازمان بزرگ، یک نفر واسطه وجود دارد یا نه.

**نتیجه:** تصمیم‌های میلیونی بر پایهٔ حدس و خاطره گرفته می‌شود.

---

## ۳. SRIP چه کار می‌کند

سه کار، به همین ترتیب:

### ۱) ثبت می‌کند — «حافظهٔ سازمان»

هر تماس، جلسه، ایمیل، قول، اقدام، سند و یادداشت روی **نام دو طرفِ رابطه** ثبت می‌شود.
نه در پوشه‌های پراکنده، نه در گوشیِ یک نفر. با این جزئیات که چه کسی مالک رابطه است، چه کسی جانشین اوست،
چه کسی چه چیزی را قول داده و تا کِی.

### ۲) نمره می‌دهد — «سنجهٔ رابطه»

به هر رابطه و به هر شرکت و به هر آدمِ کلیدی، امتیاز صفر تا صد داده می‌شود:
چقدر سالم است؟ چقدر پرریسک است؟ چقدر برایش ارزش استراتژیک داریم؟ چقدر به آدم‌های درست دسترسی داریم؟
چقدر قول‌هایش را عملی کرده‌ایم؟ **و مهم‌تر از همه: چرا این نمره را گرفته است.**

### ۳) پیگیر می‌شود — «قدم بعدی»

سامانه منتظر نمی‌ماند کسی سؤال بپرسد. می‌گوید:

> «این رابطه ۷۴ روز است بی‌خبر مانده — یک تماس برنامه‌ریزی کن.»
> «ریسک این رابطه از ۵۵ به ۷۱ رسیده — مرور و اقدام اصلاحی لازم است.»
> «به‌تنه‌ای فقط یک نفر با این سازمان در ارتباط است — ریسک تک‌مخاطب.»
> «برای رسیدن به آن وزارتخانه، شرکت «ایکس» می‌تواند معرف شما باشد.»

هر توصیه با دلیل و شواهدش می‌آید، و **انسان تصمیم می‌گیرد** که قبول کند، رد کند، ویرایش کند یا موکول کند.
اگر قبول شد، همان‌جا یک «اقدام» با مهلت برای یک نفر ساخته می‌شود.

---

## ۴. چیزی که آن را از یک CRM متمایز می‌کند

| CRM معمولی | SRIP |
|---|---|
| واحد کاری: فرصت فروش (deal) | واحد کاری: **رابطه** — بلندمدت، بین‌سازمانی، بدون تاریخ انقضا |
| هدف: بستن فروش | هدف: **سلامت و ارزش شبکه**، و از آن فروش هم درمی‌آید |
| دادهٔ افرادِ داخل تیم فروش | دادهٔ **کل شرکت‌های یک هلدینگ**، با مرز دسترسی هر شرکت |
| گزارش تعداد تماس و فاز فروش | **تحلیل شبکه**: چه کسی پل است، کجا گلوگاه است، کجا اگر یک نفر برود شبکه می‌شکند |
| یادآوریهای دستی | توصیه‌های **خودکار با دلیل و شاهد**، و گردش تأیید انسانی |
| مالکیت داده با فروشنده | **حافظهٔ نهادی**: رفتن افراد، رابطه را نمی‌بَرد |

یک تشبیه که در جلسه خوب کار می‌کند:

> CRM دفتر فروش است. SRIP **دفتر کل روابط** است — و دفتر کل، سند حسابداریِ اعتماد است.

---

## ۵. نمره‌دهی چطور کار می‌کند (بدون ریاضی)

خیلی‌ها وقتی می‌شنوند «نمرهٔ سلامت» می‌ترسند که عددی سلیقه‌ای و جعبه‌سیاه باشد. اینجا برعکس است:
**هر نمره از چند عامل ساده ساخته می‌شود و همهٔ آن عوامل در دسترس‌اند.**

برای یک رابطه، این‌ها وارد نمره می‌شود:

- **تازگی:** از آخرین تعامل چقدر گذشته است. (رابطه مثل باغبانی است؛ آبی ندهی خشک می‌شود.)
- **تداوم:** چند بار و چند جلسه در شش ماه گذشته.
- **تنوع:** فقط یک نفر درگیر است یا چند نفر با چند نقش مختلف؟ (تک‌مخاطب = شکنندگی.)
- **بازخورد:** از آن تماس‌ها چندتا نتیجهٔ مشخص داشتند، نه فقط «زنگ زدیم».
- **وفای به قول:** قول‌هایی که دادیم، به‌موقع انجام شد یا نه.
- **ارزش اقتصادی:** اندازهٔ پولی فرصت‌های ثبت‌شده روی همین رابطه.
- **اعتماد، دسترسی، نفوذ، اهمیت استراتژیک:** قضاوت‌های ثبت‌شدهٔ انسانی که مدیر رابطه پر می‌کند.
- **ریسک:** که در نمرهٔ سلامت منفی حساب می‌شود.

همه جمع می‌شوند و یک عدد ۰ تا ۱۰۰ می‌دهند. **با یک نکتهٔ مهم:**

> **وزنِ هر عامل، تنظیم‌شدنی است.**

یعنی اگر نظر هیئت‌مدیره این است که برای روابطِ بانک‌ها «ریسک» و «حجم پول» مهم‌تر از «تعداد جلسه» باشد،
مدیر سامانه همان را از صفحهٔ تنظیمات عوض می‌کند؛ بدون تغییر نرم‌افزار، بدون آپدیت، بدون توقف کار.
و چون هر تغییر یک **نسخهٔ جدید** می‌سازد، بعداً می‌توان گفت:
«این نمره با مدل شمارهٔ ۳ حساب شده؛ ماه پیش با مدل ۲، هفت نمره بالاتر بود.»

این همان چیزی است که باعث می‌شود مدیر بتواند به عدد اعتماد کند: **قابل توضیح، قابل بازگشت، قابل مقایسه در طول زمان.**

چند نمرهٔ دیگر هم هست که همین منطق را دارند:

- **نمرهٔ ریسک رابطه:** ریسک اعلام‌شده + شکنندگی + بی‌خبری طولانی.
- **نمرهٔ شبکهٔ هر شرکت:** چند رابطه دارد، چقدر سالم‌اند، چند فرصت، چند آدم کلیدی — «دارایی رابطه‌ای» آن شرکت.
- **نمرهٔ اتصال‌دهندهٔ هر آدم:** چه کسی گرهٔ اتصال ماست؛ آدمی که ۴۰ رابطهٔ خوب دارد و همیشه معرفی‌هایش به نتیجه می‌رسد.
- **نمرهٔ فرصت:** چقدر محتمل است، چقدر بزرگ است، و رابطه‌اش چقدر به اندازهٔ کافی گرم است.
- **سنجش کیفیت داده:** چقدر از روابط مسئول دارند، چقدر اطلاعات ناقص است، چقدر داده تکراری داریم. بدون این، بقیهٔ نمره‌ها روی شن ساخته می‌شوند.

---

## ۶. هوشمندی سامانه کجاست؟

اینجا صادقانه‌ترین بخش ارائه است، و معمولاً سؤال اول خریدار هم همین است.

**هوشمندی SRIP در نسخهٔ فعلی، «هوش قاعده‌محور» است، نه هوش مصنوعی زبانی.**
یعنی:

- پرسش‌ها را از روی **دادهٔ واقعیِ خودتان** و در چارچوب دسترسی خودتان جواب می‌دهد.
- برای «قدم بعدی من چیست؟» برمی‌گردد به تقویم روابط، جلسات بی‌نتیجه، قول‌های موکول‌شده و روابط سردشده — و چند پیشنهاد مشخص، بدون تکرار، هرکدام با دلیل.
- برای «ریسک‌ها کجا هستند؟» هم سیگنال‌های متنیِ نوشته‌ها را می‌بیند و هم زنگ‌های داده‌ای (ریسک بالا، سلامت پایین، تک‌مخاطب).
- از اسناد و پیوست‌ها هم می‌تواند تکه‌های مرتبط را بیرون بیاورد.

و سه قاعدهٔ ایمنی دارد که در جلسه خریدار خوشایند است:

1. **قبل از پاسخ، اجازه را چک می‌کند.** شما فقط آنچه خودتان مجازید را می‌بینید؛ حتی در جستجو و حتی در بریف مدیریتی.
2. **اطلاعات حساس از متن پاک می‌شود** (ایمیل و شماره تماس) پیش از هر پردازش؛ و تلاش‌های دست‌کاریِ دستوری («دستورهای قبلی را فراموش کن») خنثی می‌شوند.
3. **خودسرانه چیزی را ثبت یا اعمال نمی‌کند.** نتیجهٔ پیشنهادی برچسب «نیازمند تأیید شما» می‌خورد.

**فراخوانی مدل زبانی بیرونی اختیاری است و پیش‌فرض خاموش است.** یعنی در نسخهٔ فعلی هیچ داده‌ای به بیرون نمی‌رود؛
اگر سازمانی خواست، با یک تنظیمات فعال می‌شود (و آن وقت هم پاسخ مدل، جای داده‌های شما را نمی‌گیرد).

### بریف هفتگی مدیران

یک خروجی آماده وجود دارد که هفتگی برای مدیر تولید می‌شود: جلسات این هفته، قول‌های باز، اقدامات عقب‌افتاده،
روابطی که به منطقهٔ خطر رفته‌اند، فرصت‌های تازه، و سه توصیهٔ روشن — همراه با لینک به خودِ رکوردها تا قابل پیگیری باشد.
ارائه‌اش در جلسهٔ هیئت‌مدیره ده دقیقه وقت می‌گیرد و معمولاً همین، نقطهٔ فروش داخلی شماست.

---

## ۷. شبکه — بخشی که هیچ CRM‌ای ندارد

این قسمت را اگر روی صفحه نشان دهید، جلسه عوض می‌شود.

سامانه یک نقشهٔ گرافیکی از شبکهٔ روابط می‌سازد: هر سازمان یک خوشه، آدم‌ها و پروژه‌ها اطرافش،
خطوط رابطه با رنگ سلامت و ضخامت قدرت. روی همین نقشه:

- **مسیر‌یابی:** «از ما تا آن سازمان چهار خط فاصله است یا یک معرفی؟» سامانه کوتاه‌ترین و **بهترین** مسیر را می‌دهد —
  بهترین یعنی از خطوطی که سالم‌تر و کم‌ریسک‌ترند، نه فقط کم‌تعدادترین.
- **پل‌ها و گلوگاه‌ها:** کدام آدم یا کدام شرکت، نقطهٔ عبور اجباری است.
- **نقطهٔ شکست:** اگر این یک نفر برود یا این رابطه قطع شود، شبکهٔ ما چند تکه می‌شود؟ (سامانه واقعاً می‌سنجد، نه حدس.)
- **اتصال‌دهنده‌ها:** چه کسانی در داخل خودمان، درِ بیرون را باز می‌کنند.

**کاربرد عملیِ «تطبیق نیازمندی با شبکه»:** شما یک نیاز پروژه تعریف می‌کنید — مثلاً «تأمین‌کنندهٔ اعتباری برای
پروژهٔ فولاد در خراسان». سامانه واژگان نیاز را برمی‌دارد، سازمان‌های مناسب را می‌یابد، و برای هرکدام می‌گوید:
**اتصال مستقیم داریم / با یک واسطه می‌رسیم / اصلاً رابطه‌ای نداریم (و این یک شکاف رشد است)**.
برای هر مورد، «احتمال موفقیت» می‌دهد بر پایهٔ تناسب هدف و قدرت مسیر، و آدمِ مناسبِ معرف را هم معرفی می‌کند.
فهرست شکاف‌ها در عمل می‌شود **نقشهٔ راه توسعهٔ رابطه** برای شش ماه آینده.

---

## ۸. امنیت و مرزها (آنچه خریدار سازمانی می‌پرسد)

- **ورود:** رمز عبور با قفل‌شدن پس از چند تلاش خطا، ورود دومرحله‌ای، ورود با حساب سازمانی (یک‌بار‌ورودی) و
  نشست‌های قابل‌باطل‌کردن تک‌به‌تک — مدیر می‌تواند ببیند حساب شما روی چه دستگاه‌هایی باز است و قطعش کند.
- **دسترسی بر پایهٔ نقش و محدوده:** ده نقش از مدیر کل سیستم تا فقط‌خواندنی؛ هر کاربر در هر شرکت یک نقش دارد.
  مدیر هلدینگ کل گروه را می‌بیند، مدیر شرکت فقط شرکت خودش را.
- **طبقه‌بندی محرمانگی:** هر رکورد برچسب حساسیت دارد (از عمومی تا بسیارمحرمانه) و کاربر تا سقف مشخص‌شده‌اش
  را می‌بیند. فیلدهای فوق‌حساس (مثل شمارهٔ تماس شخصی یا یادداشت‌های داخلی) اجازهٔ جداگانه می‌خواهند.
- **مالکیت رابطه در دو طرف:** برای دیدن یک رابطه باید به یکی از دو طرف دسترسی داشته باشید؛ این باعث می‌شود
  شرکت زیرمجموعه، دادهٔ رابطهٔ شرکت دیگر را بی‌اجازه نبیند.
- **ممیزی کامل:** هر تغییر — چه کسی، چه چیزی، قبلش چه بود، بعدش چه شد، و چرا. خروج گرفتن داده هم ثبت می‌شود
  و در تنظیمات سخت‌گیرانه نیازمند تأیید است.
- **حریم خصوصی:** درخواست دسترسی/حذف اطلاعات ثبت می‌شود، جریان تأیید دارد، و حذف واقعی فقط با تأییدیهٔ دوم انجام می‌شود.
- **پشتیبان‌گیری و بازیابی:** نسخهٔ پشتیبان رمزنگاری‌شده با اثر انگشت، زمان‌بندی، و **تمرین بازیابی دوره‌ای** —
  چون پشتیبانی که باز نشده، پشتیبانی نیست.

---

## ۹. کجا می‌شود استفاده‌اش کرد (سناریوهای واقعی)

1. **صبح مدیر روابط:** سه توصیهٔ باز، دو اقدام معوق، یک جلسهٔ بی‌نتیجهٔ هفتهٔ پیش. کار امروز را از همین‌جا شروع می‌کند.
2. **جلسهٔ هیئت‌مدیره:** بریف هفتگی روی پرده؛ سؤال «چرا نمرهٔ این رابطه افت کرد؟» با چهار عامل توضیح داده می‌شود.
3. **مذاکرهٔ بانکی:** قبل از جلسه، پروندهٔ رابطه، سابقهٔ قول‌ها، و اینکه چه کسی قبلاً نتیجه را باز کرده است.
4. **ورود مدیر جدید:** دو هفته به‌جای دو سال — تاریخچهٔ هر رابطه در سامانه است، نه در سرِ نفر رفته.
5. **شکاف شبکه:** «برای ورود به آن بازار، سه سازمان هدف داریم که هیچ خطی به آن‌ها نداریم» — تبدیل به تکلیف.
6. **حاکمیت داده:** گزارش کیفیت: چه روابطی بی‌صاحب‌اند، چه مخاطبانی ایمیل نامعتبر دارند، چه قول‌هایی سررسید ندارند.

---

## ۱۰. الان چه چیزی آماده است

- هستهٔ کامل ثبت روابط و حافظهٔ تعاملات: سازمان‌ها، اشخاص، روابط، تعاملات، جلسات، اقدام‌ها، قول‌ها، پروژه‌ها،
  نیازمندی‌ها، فرصت‌ها، معرفی‌ها، اسناد و برچسب‌ها.
- پنج خانوادهٔ امتیاز + نسخه‌بندی، کالیبراسیون و تاریخچهٔ نمرات.
- تحلیل شبکه: نقشهٔ گرافیکی، مسیر‌یابی، پل‌ها، گلوگاه‌ها، نقاط شکست، اتصال‌دهنده‌ها.
- موتور توصیه با تأیید انسانی، تبدیل توصیه به اقدام، و اندازه‌گیری اینکه چند درصد توصیه‌ها واقعاً نتیجه دادند.
- اعلان‌های هوشمند با قواعد قابل‌تنظیم، هشدارهای آماده (سرد شدن رابطه، قول معوق، جلسهٔ بی‌نتیجه، ریسک تک‌مخاطب،
  افت امتیاز، تغییر سمت افراد)، و ارسال بلادرنگ.
- گردش کار و مرکز تأییدها (درخواست‌های تأیید چندمرحله‌ای).
- ورود اطلاعات دسته‌جمعی با تشخیص تکراری، گزارش خطا و تأیید نهایی؛ گزارش‌گیری با خروجی اکسل/پی‌دی‌اف/متن.
- جستجوی سراسری در همه‌چیز، سازگار با فارسی.
- **رابط وب فارسی و راست‌به‌چپ** با تقویم جلالی، پوستهٔ روشن/تیره، پیشخوان مدیریتی، و دو نسخهٔ ظاهری برای مقایسه.
- **اپلیکیشن موبایل** برای ثبت در بیرون از دفتر، با کار در حالت آفلاین و ارسال خودکار پس از اتصال (بدون ثبت تکراری).
- لایهٔ پایش سلامت سامانه، محدودسازی درخواست‌ها، و آماده‌سازی زیرساخت ابری با فایروال.
- یک **نسخهٔ نمایشی کامل** که در مرورگر اجرا می‌شود و دادهٔ نمونه دارد — برای دیدن محصول بدون نصب هیچ چیزی.

---

## ۱۱. چه چیزهایی باید اضافه شود

فهرست صادقانه، با اولویت. این‌ها «نقص» نیستند، **فاز بعدی** هستند.

### الف) برای اینکه واقعاً استفاده روزانه شود (اولویت ۱)

| چه چیزی | چرا لازم است |
|---|---|
| **اتصال به ایمیل و تقویم** | امروز ثبت تعامل دستی است؛ بزرگ‌ترین مانع ورود داده همین است. با همگام‌سازی، سامانه خودش جلسه و نامه را ثبت می‌کند. |
| **ثبت سریع از موبایل (ویجت/فرم یک‌صفحه‌ای)** | مدیر رابطه بعد از جلسه در ماشین است، نه پشت لپ‌تاپ. |
| **تکمیل رابط موبایل برای صفحه‌های کوچک** | چیدمان موبایل امروز در چند صفحه به‌هم‌ریخته است (منوی کشویی، اندازهٔ دکمه‌ها، فاصله‌های امن پایین/بالای صفحه). این یک کار کوتاه و پردامنه است. |
| **نصب‌پذیری به‌عنوان اپ (افزودن به صفحهٔ اصلی)** | تا امروز سامانه در مرورگر باز می‌شود؛ برای استفادهٔ روزانه باید مثل اپ رفتار کند. |
| **یادآوری هفتگی در ایمیل/پیام‌رسان** | تا توصیه‌ها را نبینند، اقدامی ساخته نمی‌شود. چرخهٔ ارزش همین‌جا می‌شکند. |
| **تازه‌سازی خودکار امتیازها در شب** | امروز نمره‌ها با ثبت داده یا درخواست دستی حساب می‌شوند؛ «سرد شدن رابطه» باید خودش هر شب محاسبه شود تا صبح عدد درست باشد. |

### ب) برای اینکه مدیرعامل بگوید «این یعنی پول» (اولویت ۲)

- **ارزش دلاری شبکه:** تبدیل امتیاز رابطه به برآورد ارزش (ریالی/دلاری) و نشان دادن روند آن در زمان.
  امروز سلامت را می‌سنجیم؛ هنوز به زبان پول ترجمه نکرده‌ایم.
- **پیش‌بینی ریزش رابطه:** با تاریخچهٔ نمرات و نتایج گذشته، مدل یاد بگیرد کدام رابطه‌ها در آستانهٔ سردشدن‌اند.
- **سنجهٔ بازگشت سرمایهٔ رابطه:** هزینهٔ نگه‌داشتن (زمان، هدیه، سفر) در برابر نتیجهٔ کسب‌شده.
- **مقایسهٔ شرکت‌های هلدینگ:** کدام شرکت زیرمجموعه شبکهٔ قوی‌تر و سالم‌تری دارد — یک رقابت سالم و سازنده.
- **داشبورد BI زنده و خروجی خودکار برای جلسه** (ارسال زمان‌بندی‌شدهٔ PDF به صندوق ایمیل هیئت‌مدیره).

### ج) برای هوشمندی بهتر (اولویت ۳)

- **جستجوی معنایی واقعی** به‌جای تطبیق واژه‌ها، تا «مشکل در قرارداد آریا» را بفهمد حتی اگر کلمهٔ «قرارداد» نبود.
- **خلاصه‌سازی خودکار جلسات** از متن یا پیاده‌سازی ضبط جلسه، و استخراج خودکار قول‌ها (با تأیید انسانی).
- **اعتبارسنجی پاسخ مدل:** هر پاسخ هوشمند باید به رکورد منبع لینک بدهد و اگر شواهد کافی ندارد، صریح بگوید «نمی‌دانم».
- **پیشنهاد گام بعدی شخصی‌سازی‌شده:** یاد بگیرد کدام نوع توصیه را این مدیر معمولاً می‌پذیرد تا همان‌ها را بالا بیاورد.

### د) برای فروش به سازمان‌های بزرگ‌تر (اولویت ۴)

- **ورود با حساب سازمانی به‌صورت استاندارد** و تک‌خروجی (پروایژن خودکار حساب از اکتیودرکتور).
- **برندسازی سفارشی برای هر شرکت** (لوگو، رنگ، دامنه) و فضای آزمایشی برای مشتری تازه.
- **پایگاه دانش و آموزش درون‌برنامه‌ای** + راهنمای گام‌به‌گام ورود (onboarding) — بدون این، پیاده‌سازی ماه‌ها طول می‌کشد.
- **پشتیبان‌گیری آزمون‌شده و گزارش بازیابی** به‌صورت خودکار و قابل ارائه به ممیز.
- **بازگرداندن مستندات فنی:** دفترچهٔ معماری و چک‌لیست پیاده‌سازی در نسخهٔ فعلی زیر پوشهٔ مستندات گم شده‌اند
  (محلشان را فایل خروجی نمایشی گرفته)؛ قبل از هر تحویل باید جدا و برگردانده شوند. به‌علاوهٔ یک **دفترچهٔ کاربر فارسی**
  که هنوز نوشته نشده — بدون آن، هر مشتری تازه از صفر سؤال می‌پرسد.
- **تست خودکار رابط موبایل و وب** تا هر تغییر ظاهری، چیزی را بی‌سروصدا نشکند.

---

## ۱۲. اگر بخواهید سه دقیقه توضیح دهید

> شرکت‌های ما دارایی‌ای دارند که در هیچ گزارش مالی نیست: رابطه‌ها. امروز این دارایی در سر ده نفر است و با رفتن آن‌ها می‌رود.
> SRIP هر تماس، جلسه و قول را روی نامِ خودِ رابطه ثبت می‌کند، به هر رابطه نمرهٔ سلامت و ریسک می‌دهد — نمره‌ای که
> می‌شود توضیحش داد و وزنش را عوض کرد — و بعد هر روز می‌گوید کجا باید پیگیری کرد، کجا ریسک داریم و کجا برای رسیدن به
> یک سازمان مهم، فقط یک معرفی لازم است. تصمیم از آنِ انسان است؛ سامانه دلیل و شاهد را روی میز می‌گذارد.
> همه‌چیز با مرز دسترسی هر شرکت، ممیزی کامل از هر تغییر، و پشتیبان‌گیری آزمون‌شده.
> روی موبایل هم کار می‌کند و در قطعی اینترنت، ثبت را نگه می‌دارد و بعداً بی‌تکراری می‌فرستد.
> فاز بعدی: اتصال به ایمیل و تقویم، ارزش‌گذاری ریالی شبکه، و پیش‌بینی رابطه‌های در آستانهٔ ریزش.

---

## ۱۳. پاسخ به سه سؤال همیشگی

**«اینکه همان CRM است؟»**
نه. CRM یک معامله را تا بستن دنبال می‌کند؛ SRIP یک رابطه را تا نسل‌ها. واحد کاری، اندازه‌گیری، و گزارش‌هایش فرق دارد —
به‌ویژه تحلیل شبکه که در CRM وجود ندارد. اگر CRM دارید، SRIP لایهٔ بالای آن است، نه جایگزینش.

**«چقدر طول می‌کشد تا پر شود؟»**
مهاجرت دسته‌جمعی از فایل اکسل با تشخیص تکراری و گزارش خطا ساخته شده. بخش سخت کار فنی نیست، سازمانی است:
مالک برای هر رابطه تعیین شود. توصیهٔ عملی: با بیست رابطهٔ مهم شروع کنید، شش هفته فقط همان‌ها را زنده نگه دارید،
بعد گسترش دهید.

**«نمره‌ها قابل اعتمادند یا سلیقه‌ای؟»**
هر نمره قابل بازکردن است: چه عواملی، با چه وزنی، در کدام نسخهٔ مدل. اگر مدیری بگوید وزن‌ها درست نیست،
همان‌جا عوض می‌شود و نسخهٔ قبلی برای مقایسه باقی می‌ماند. و سامانه می‌پرسد «می‌دانم یا نه» — بدون شواهد، توصیه‌ای ساخته نمی‌شود.

---

## ۱۴. جمع‌بندی یک‌خطی

**SRIP، حسابداریِ اعتماد است: رابطه‌ها را ثبت می‌کند، اندازه می‌گیرد، به‌جا یادآوری می‌کند، و نمی‌گذارد با رفتن یک نفر، سرمایه‌ای برود.**
