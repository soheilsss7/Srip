# فهرست صفحات پلتفرم SRIP + کاربرد هر صفحه

> برای اصلاح تدریجی «دونه‌دونه». هر صفحه که با هم کامل کردیم، تیکش را می‌زنیم.
> مسیر فایل هر صفحه داخل `apps/web/app/` نوشته شده تا راحت پیدا شود.

---

## ۱) هستهٔ اصلی (Main) — نوار بالای پلتفرم

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 1 | **داشبورد** `/` | `dashboard/page.tsx` | نمای کلی: سرمایهٔ شبکه، درگیری و فعالیت، تعاملات، قیف پیشنهادها، اجرای گردش‌کارها، سلامت پلتفرم |
| 2 | **سازمان‌ها** `/organizations` | `organizations/page.tsx` | فهرست سازمان‌ها با فیلتر نوع (هلدینگ/زیرمجموعه/مشتری/شریک/دولتی/سرمایه‌گذار/تأمین‌کننده) + جستجو |
| 3 | **پروفایل سازمان** `/organizations/[id]` | `organizations/[id]/page.tsx` | جزئیات سازمان: گراف اختصاصی، اعضا، واحدها، راه‌های تماس، خط زمانی |
| 4 | **اشخاص** `/people` | `people/page.tsx` | فهرست اشخاص: امتیاز نفوذ، قدرت تصمیم، فیلتر وضعیت، ثبت شخص جدید |
| 5 | **پروفایل شخص** `/people/[id]` | `people/[id]/page.tsx` | جزئیات شخص: گراف ارتباطات، امتیازها، راه‌های تماس، پیشنهاد ارتباط جدید |
| 6 | **روابط** `/relationships` | `relationships/page.tsx` | فهرست روابط بین‌سازمانی: سلامت، ارزش راهبردی، ریسک، مالک، مرتب‌سازی + ایجاد رابطه |
| 7 | **پروفایل رابطه** `/relationships/[id]` | `relationships/[id]/page.tsx` | جزئیات رابطه: وضعیت، مرحلهٔ چرخهٔ زندگی، امتیاز، بایگانی/بازیابی |
| 8 | **شبکه اطلاعاتی** `/network` | `network/page.tsx` | گراف تعاملی گره‌ها و خط‌ها: فیلتر، مسیر، تحلیل ریسک/نفوذ، وضعیت رابطه روی خط‌ها |
| 9 | **تعاملات** `/interactions` | `interactions/page.tsx` | خط زمانی تماس‌ها/ایمیل‌ها/جلسات/یادداشت‌ها + ثبت تعامل + پیگیری‌ها |
| 10 | **جزئیات تعامل** `/interactions/[id]` | `interactions/[id]/page.tsx` | جزئیات یک تعامل: خلاصه، نتیجه، وضعیت پیگیری، ویرایش/حذف |

## ۲) هوشمندی و عملیات (Intelligence)

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 11 | **جلسات** `/meetings` | `meetings/page.tsx` | فهرست جلسات (همه/آینده)، ثبت نتیجهٔ سریع، آمار، برنامه‌ریزی جلسه جدید |
| 12 | **جزئیات جلسه** `/meetings/[id]` | `meetings/[id]/page.tsx` | شرکت‌کنندگان، صورتجلسه، اقدامات مرتبط، ثبت خروجی/یادداشت/تصمیم‌ها، استخراج اقدامات |
| 13 | **اقدامات** `/actions` | `actions/page.tsx` | فهرست اقدامات عملیاتی: مالک، موعد، یادآور، اولویت + ایجاد/ویرایش |
| 14 | **جزئیات اقدام** `/actions/[id]` | `actions/[id]/page.tsx` | جزئیات اقدام، تغییر وضعیت، وابستگی‌ها، حذف |
| 15 | **تعهدات** `/commitments` | `commitments/page.tsx` | فهرست تعهدات: مالک، موعد، ریسک + ایجاد |
| 16 | **جزئیات تعهد** `/commitments/[id]` | `commitments/[id]/page.tsx` | جزئیات تعهد، تغییر وضعیت، علامت‌گذاری عقب‌افتاده |
| 17 | **پروژه‌ها** `/projects` | `projects/page.tsx` | فهرست پروژه‌ها: هدف، سازمان، مالک، وضعیت + ایجاد |
| 18 | **فضای کاری پروژه** `/projects/[id]` | `projects/[id]/page.tsx` | الزامات، ریسک‌ها (احتمال×اثر)، مراحل کلیدی، روابط مرتبط |
| 19 | **فرصت‌ها** `/opportunities` | `opportunities/page.tsx` | فهرست فرصت‌های تجاری: ارزش، احتمال + ایجاد |
| 20 | **جزئیات فرصت** `/opportunities/[id]` | `opportunities/[id]/page.tsx` | جزئیات فرصت: ارزش، احتمال، وضعیت، ویرایش |
| 21 | **هوشمندی شبکه** `/intelligence` | `intelligence/page.tsx` | سلامت روابط، ریسک، فرصت، پوشش راهبردی، نسخه‌بندی امتیازدهی |
| 22 | **دستیار هوشمند** `/ai` | `ai/page.tsx` | ۹ قابلیت پرس‌وجو (جستجوی هوشمند، بریف جلسه، خلاصه‌سازی، استخراج اقدام/تعهد، ریسک/فرصت، بهترین اقدام بعدی…) |
| 23 | **گزارش هفتگی راهبردی** `/ai-executive-brief` | `ai-executive-brief/page.tsx` | بریف هفتگی: جلسات، فرصت‌های جدید، تعهدات باز، اقدامات عقب‌افتاده، ریسک‌ها |
| 24 | **پیشنهادهای هوشمند** `/recommendations` | `recommendations/page.tsx` | فهرست پیشنهادها: تأیید/رد/تعویق/اجرا/واگذاری/بایگانی |
| 25 | **جزئیات پیشنهاد** `/recommendations/[id]` | `recommendations/[id]/page.tsx` | جزئیات پیشنهاد: شواهد، اطمینان، اقدام‌ها (تأیید/رد/تعویق/واگذاری/ویرایش) |
| 26 | **گزارش‌ها** `/reports` | `reports/page.tsx` | خروجی گزارش‌ها (CSV/PDF/XLSX): روابط، سازمان‌ها، جلسات، تعهدات، اقدامات، فرصت‌ها، شبکه… |
| 27 | **خروجی گزارش** `/reports/export` | `reports/export/page.tsx` | فرم خروجی با تأیید دوم‌نفره: انتخاب نوع گزارش + فرمت + دانلود |

## ۳) سیستمی (System)

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 28 | **اسناد** `/documents` | `documents/page.tsx` | بارگذاری امن، اسکن بدافزار، ایندکس محتوا، دانلود امضاءشده (دانش سازمانی) |
| 29 | **مرکز اعلان** `/notifications` | `notifications/page.tsx` | اعلان‌ها + تنظیم ترجیحات کانال (درون‌برنامه‌ای/ایمیل/فشاری) + خلاصهٔ دوره‌ای + گزارش تحویل |
| 30 | **جستجوی سراسری** `/search` | `search/page.tsx` | جستجو در سازمان/شخص/رابطه/جلسه/تعامل/پروژه/فرصت/سند + ذخیرهٔ جستجوها |
| 31 | **تقویم جلسات** `/calendar` | `calendar/page.tsx` | نمای ماهانهٔ جلسات، پیمایش ماه‌ها، لینک به جلسات |
| 32 | **تبادل داده** `/data-exchange` | `data-exchange/page.tsx` | خروجی فهرست‌ها (CSV) + ورود گروهی اشخاص از قالب CSV با پیش‌نمایش |
| 33 | **تطبیق نیازمندی** `/requirements` | `requirements/page.tsx` | برای یک نیازمندی پروژه، پوشش ارتباطی (رابطه‌به‌نیازمندی) را محاسبه می‌کند |
| 34 | **معرفی‌ها** `/referrals` | `referrals/page.tsx` | مدیریت مسیرهای معرفی: معرف، فرد معرفی‌شونده، وضعیت، نتیجه |
| 35 | **تأییدها** `/approvals` | `approvals/page.tsx` | درخواست‌های تأیید (رابطهٔ حساس، تغییر امتیاز، اشتراک/ورود/خروجی/حذف داده) + ثبت درخواست جدید |
| 36 | **تنظیمات** `/settings` | `settings/page.tsx` | پوسته (روشن/تیره/سیستم)، زبان، منطقهٔ زمانی، شرکت پیش‌فرض |
| 37 | **نشست‌ها** `/sessions` | `sessions/page.tsx` | مدیریت دستگاه‌های فعال: ابطال نشست، ابطال همه به‌جز فعلی |
| 38 | **راهنما** `/help` | `help/page.tsx` | راهنمای استفاده: ورود/نقش‌ها، حوزه‌ها، هوش مصنوعی، امنیت، سؤالات متداول |

## ۴) مدیریت (Admin)

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 39 | **مدیریت سیستم** `/admin` | `admin/page.tsx` | مرکز مدیریت: کاربران، نقش‌ها، مجوزها، برچسب‌ها، فیلدهای سفارشی، امتیازدهی، قواعد اعلان، یکپارچه‌سازی، ممیزی |
| 40 | **کاربران** `/admin/users` | `admin/users/page.tsx` | فهرست کاربران: ایمیل، نام، وضعیت فعال، تأیید ایمیل |
| 41 | **نقش‌ها** `/admin/roles` | `admin/roles/page.tsx` | مدیریت نقش‌ها (کلید، نام، توضیح) |
| 42 | **مجوزها** `/authorization` | `authorization/page.tsx` | فهرست مجوزهای سرور و نقش‌های صاحب هرکدام (مبنای RBAC) |
| 43 | **برچسب‌ها** `/admin/tags` | `admin/tags/page.tsx` | مدیریت برچسب‌ها |
| 44 | **فیلدهای سفارشی** `/admin/custom-fields` | `admin/custom-fields/page.tsx` | تعریف فیلد سفارشی برای نهادها (سازمان/شخص/رابطه/…) |
| 45 | **قواعد امتیازدهی** `/admin/scoring` | `admin/scoring/page.tsx` | فهرست قواعد امتیازدهی |
| 46 | **قواعد اعلان** `/admin/notification-rules` | `admin/notification-rules/page.tsx` | قواعد اعلان: رویداد، کانال‌ها، فعال بودن |
| 47 | **یکپارچه‌سازی (مدیریتی)** `/admin/integrations` | `admin/integrations/page.tsx` | وضعیت اتصال سرویس‌های سازمانی |
| 48 | **لاگ ممیزی** `/admin/audit` | `admin/audit/page.tsx` | ثبت کامل فعالیت‌ها: عملیات، نهاد، کاربر، دلیل، IP، زمان |
| 49 | **داده‌های مبنایی** `/admin/master-data` | `admin/master-data/page.tsx` | انواع رابطه و انواع تعامل (مرجع) |
| 50 | **کنترل خروجی داده** `/admin/exports` | `admin/exports/page.tsx` | تاریخچه و کنترل خروجی‌های حساس (محدوده/تأیید/ممیزی) |
| 51 | **پرچم‌های ویژگی** `/admin/feature-flags` | `admin/feature-flags/page.tsx` | فعال‌سازی کنترل‌شدهٔ قابلیت‌ها با محدودهٔ سازمانی |
| 52 | **نگهداری داده** `/admin/retention` | `admin/retention/page.tsx` | پیش‌نمایش و اجرای نگهداشت/چرخهٔ حیات داده |
| 53 | **مدیریت نشست‌ها** `/admin/sessions` | `admin/sessions/page.tsx` | ابطال مدیریتی نشست هر کاربر (شناسه کاربر + شناسه نشست) |

## ۵) داده و کیفیت

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 54 | **مدیریت داده** `/data-management` | `data-management/page.tsx` | نمای کلی کیفیت: تکراری‌ها، بدون مالک، روابط کهنه، ایمیل نامعتبر، پروفایل ناقص |
| 55 | **ورود داده** `/data-management/import` | `data-management/import/page.tsx` | ورود گروهی: بارگذاری ← نگاشت ← اعتبارسنجی ← تکراری ← پیش‌نمایش ← تأیید ← ورود ← گزارش |
| 56 | **کیفیت داده (مدیریتی)** `/data-management/quality` | `data-management/quality/page.tsx` | بازبینی کیفیت و محاسبهٔ مجدد |
| 57 | **کیفیت داده** `/data-quality` | `data-quality/page.tsx` | نمای کیفی: تکراری، بدون مالک/مخاطب، کهنه، ایمیل نامعتبر، تاریخ ازدست‌رفته |
| 58 | **چرخهٔ حیات داده** `/data-lifecycle` | `data-lifecycle/page.tsx` | سوابق چرخهٔ حیات، توزیع وضعیت/نهاد، تأییدیه‌های حذف دائمی |

## ۶) امنیت، حریم خصوصی و حاکمیت

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 59 | **امنیت و حاکمیت** `/security` | `security/page.tsx` | بررسی مقدماتی حاکمیت (Preflight)، ممیزی خروجی، رویدادهای امنیتی |
| 60 | **رویدادهای امنیتی** `/security-events` | `security-events/page.tsx` | رخدادها: دسترسی مشکوک، تغییر توکن، ایجاد خروجی + شدت و نهاد |
| 61 | **حریم خصوصی** `/privacy` | `privacy/page.tsx` | خط‌مشی‌های پردازش، رضایت‌ها، درخواست‌های حریم خصوصی (دسترسی/خروجی/پاک‌سازی) |
| 62 | **حاکمیت داده** `/governance` | `governance/page.tsx` | بررسی‌های مقدماتی حاکمیت (خط‌مشی/خروجی/رویداد امنیتی/کنترل سازمانی) |
| 63 | **حاکمیت سازمانی** `/enterprise` | `enterprise/page.tsx` | خط‌مشی سراسری (ABAC)، پرچم‌های ویژگی، مستندات طبقه‌بندی‌شده، رویدادهای امنیتی |
| 64 | **یکپارچه‌سازی** `/integrations` | `integrations/page.tsx` | اتصال تقویم/ایمیل (گوگل/مایکروسافت): OAuth، همگام‌سازی، تاریخچهٔ اجرا |

## ۷) عملیاتی و فنی (Operational)

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 65 | **تحلیل محصول** `/analytics` | `analytics/page.tsx` | مصرف قابلیت‌ها، کاربران فعال، شبکهٔ راهبردی، اجرای گردش کار، قیف پیشنهادها |
| 66 | **سنجه‌ها** `/metrics` | `metrics/page.tsx` | شاخص‌های عملیاتی: حجم درخواست‌ها، خطاها، تأخیرها، دسترس‌پذیری |
| 67 | **مشاهده‌پذیری** `/observability` | `observability/page.tsx` | تأخیر API/پایگاه داده/صف + داده‌سنجی زمان اجرا |
| 68 | **پایش** `/monitoring` | `monitoring/page.tsx` | زمان پاسخ API بر اساس مسیر (نمودار توزیع) |
| 69 | **سلامت و آمادگی** `/health` | `health/page.tsx` | وضعیت Liveness/Readiness و وابستگی‌های زمان اجرا |
| 70 | **پوشش API** `/api-coverage` | `api-coverage/page.tsx` | رجیستری زندهٔ نقاط اتصال از قرارداد OpenAPI |
| 71 | **پوشش سرور ← رابط کاربری** `/backend-coverage` | `backend-coverage/page.tsx` | ممیزی تطابق کنترلرهای سرور با صفحات |
| 72 | **مرکز عملیات** `/workspace` | `workspace/page.tsx` | نقطهٔ ورود یکپارچه به تمام حوزه‌های پلتفرم |

## ۸) احراز هویت

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 73 | **ورود** `/login` | `login/page.tsx` | ورود با نام کاربری/رمز + کد دومرحله‌ای |
| 74 | **ثبت‌نام** `/register` | `register/page.tsx` | ساخت حساب جدید |
| 75 | **فراموشی رمز** `/forgot-password` | `forgot-password/page.tsx` | درخواست بازیابی رمز |
| 76 | **بازنشانی رمز** `/password-reset` | `password-reset/page.tsx` | تنظیم رمز جدید |
| 77 | **تأیید دومرحله‌ای** `/mfa` | `mfa/page.tsx` | فعال‌سازی/تأیید کد دومرحله‌ای |

## ۹) عمومی / سیستمی

| # | صفحه | مسیر فایل | کاربرد |
|---|------|-----------|--------|
| 78 | **صفحهٔ ۴۰۴** | `not-found.tsx` | خطای «صفحه پیدا نشد» |
| 79 | **صفحهٔ خطا** | `error.tsx` / `global-error.tsx` | خطای عمومی رندر |
| 80 | **بارگذاری** | `loading.tsx` | صفحهٔ در حال بارگذاری |
| 81 | **دانش (قدیمی)** `/knowledge` | `knowledge/page.tsx` | ریدایرکت به `/documents` (فقط ریدایرکت است) |
| 82 | **صفحهٔ اصلی** `/` | `page.tsx` | فقط ریدایرکت به داشبورد |

---

## خلاصهٔ گروه‌ها

- **هستهٔ اصلی:** ۱۰ صفحه (داشبورد، سازمان، شخص، رابطه، شبکه، تعامل)
- **هوشمندی و عملیات:** ۱۷ صفحه (جلسه، اقدام، تعهد، پروژه، فرصت، هوشمندی، AI، پیشنهاد، گزارش)
- **سیستمی:** ۱۱ صفحه (سند، اعلان، جستجو، تقویم، تبادل داده، نیازمندی، معرفی، تأیید، تنظیمات، نشست، راهنما)
- **مدیریت:** ۱۵ صفحه
- **داده و کیفیت:** ۵ صفحه
- **امنیت/حریم خصوصی/حاکمیت:** ۶ صفحه
- **عملیاتی و فنی:** ۸ صفحه
- **احراز هویت:** ۵ صفحه
- **عمومی:** ۵ صفحه

**جمع کل: ~۸۲ صفحه**

> ⚠️ «لیست کاربردها» از کد فعلی استخراج شد. اگر صفحه‌ای کاربردش با انتظار تو فرق دارد، در همان دستور بگو تا هنگام اصلاح، هم‌زمان رفتارش را هم اصلاح کنیم.
