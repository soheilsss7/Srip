# ADR-0002 — PostgreSQL as System of Record

Status: Accepted

Decision: PostgreSQL is the core data store and relationship graph representation for the first phases.

Reason: relational integrity, transactions, mature indexing and enough graph capability for initial workloads. A dedicated graph database requires benchmark evidence.
