# Infrastructure

Terraform modules should be added here after the cloud/provider is selected. Do not commit real credentials. The first production target should provision private networking, managed PostgreSQL, managed Redis, object storage, secret management, WAF/CDN, logging and backups.
