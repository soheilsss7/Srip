export type ParsedImportRow = Record<string,string>;
export const normalizeImportValue=(v:unknown)=>String(v??'').trim();
export const normalizeImportPhone=(v:unknown)=>normalizeImportValue(v).replace(/[^0-9+]/g,'').replace(/^00/,'+');
export const normalizeImportDomain=(v:unknown)=>normalizeImportValue(v).toLowerCase().replace(/^https?:\/\//,'').replace(/^www\./,'').split('/')[0];
export function parseCsvImport(text:string):ParsedImportRow[]{const rows:string[][]=[];let row:string[]=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(quoted){if(c==='"'){if(text[i+1]==='"'){cell+='"';i++}else quoted=false}else cell+=c}else if(c==='"')quoted=true;else if(c===','){row.push(cell);cell=''}else if(c==='\n'){row.push(cell);rows.push(row);row=[];cell=''}else if(c==='\r'){if(text[i+1]!=='\n'){row.push(cell);rows.push(row);row=[];cell=''}}else cell+=c}if(cell||row.length){row.push(cell);rows.push(row)}if(!rows.length)return[];const headers=rows.shift()!.map(normalizeImportValue);return rows.filter(r=>r.some(v=>normalizeImportValue(v))).map(r=>Object.fromEntries(headers.map((h,i)=>[h,normalizeImportValue(r[i])]))) }
export function importNameSimilarity(a:string,b:string){const x=normalizeImportValue(a).toLowerCase(),y=normalizeImportValue(b).toLowerCase();if(!x||!y)return 0;if(x===y)return 1;const d:number[][]=Array.from({length:x.length+1},(_,i)=>[i,...Array(y.length).fill(0)]);for(let j=0;j<=y.length;j++)d[0][j]=j;for(let i=1;i<=x.length;i++)for(let j=1;j<=y.length;j++)d[i][j]=Math.min(d[i-1][j]+1,d[i][j-1]+1,d[i-1][j-1]+(x[i-1]===y[j-1]?0:1));return 1-d[x.length][y.length]/Math.max(x.length,y.length)}
