'use client'; import {useEffect,useState} from 'react'; import {api} from '../_lib/api';
export default function Workflows(){const [items,setItems]=useState<any[]>([]),[error,setError]=useState('');useEffect(()=>{api('/workflows').then(v=>setItems(Array.isArray(v)?v:[])).catch(e=>setError(e.message))},[]);return <main className="shell"><header className="topbar"><strong>SRIP</strong><nav><a href="/">Dashboard</a><a href="/workflows">Workflows</a></nav></header><section className="hero"><p className="eyebrow">Phase 14 · Workflow</p><h1>Workflow Automation</h1><p>Triggers, conditions, actions, approvals and wait/resume execution.</p></section>{error&&<p className="error">{error}</p>}<section className="card"><div className="list">{items.map(w=><article className="listRow" key={w.id}><div><strong>{w.name}</strong><small>{w.entityType} · {w.isActive?'Active':'Paused'}</small></div><span>{Array.isArray(w.definition?.actions)?w.definition.actions.length:0} actions</span></article>)}</div></section></main>}
