'use client'; import {useEffect,useState} from 'react'; import {api} from '../_lib/api';
export default function Analytics(){const [data,setData]=useState<any>(); const [error,setError]=useState(''); useEffect(()=>{api('/analytics/summary').then(setData).catch(e=>setError(e.message))},[]); return <main className="shell"><header className="topbar"><strong>SRIP</strong><nav><a href="/">Dashboard</a><a href="/analytics">Analytics</a></nav></header><section className="hero"><p className="eyebrow">Phase 14 · Analytics</p><h1>Product Analytics</h1><p>Tenant-scoped usage, activity and relationship intelligence metrics.</p></section>{error&&<p className="error">{error}</p>}{data&&<><section className="grid2"><section className="card"><h2>Core Counts</h2><div className="list">{Object.entries(data.counts).map(([k,v])=><article className="listRow" key={k}><strong>{k}</strong><span>{String(v)}</span></article>)}</div></section><section className="card"><h2>Engagement</h2><div className="list"><article className="listRow"><strong>Active users (30d)</strong><span>{data.engagement.activeUsers30d}</span></article><article className="listRow"><strong>Recommendation acceptance</strong><span>{data.engagement.recommendationAcceptance}</span></article><article className="listRow"><strong>Successful connections</strong><span>{data.engagement.successfulConnections}</span></article><article className="listRow"><strong>Relationship updates</strong><span>{data.engagement.relationshipUpdates}</span></article></div></section></section><section className="card"><h2>Feature Usage</h2><div className="list">{data.engagement.featureUsage.map((x:any)=><article className="listRow" key={x.feature}><strong>{x.feature}</strong><span>{x.count}</span></article>)}</div></section></>}</main>}
