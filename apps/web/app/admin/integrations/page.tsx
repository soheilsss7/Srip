'use client';import{useEffect,useState}from'react';import{api}from'../../_lib/api';import{AdminNav,DataTable,ErrorCard,PageHeader}from'../../_components/page-ui';
export default function Integrations(){const[r,setR]=useState<any[]>([]),[e,setE]=useState('');useEffect(()=>{api<any[]>('/admin/integrations').then(v=>setR(Array.isArray(v)?v:(v as {integrations?:unknown[]}).integrations??[])).catch(x=>setE(x.message))},[]);return <main className="admin-layout"><PageHeader eyebrow="INTEGRATIONS" title="Integrations" description="وضعیت اتصال سرویس‌های سازمانی؛ Secretها در UI نمایش داده نمی‌شوند."/><AdminNav/><ErrorCard message={e}/><DataTable columns={[{key:'provider',label:'Provider'},{key:'kind',label:'Type'},{key:'status',label:'Status'},{key:'organizationId',label:'Organization'}]} rows={r}/></main>}
