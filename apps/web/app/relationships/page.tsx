'use client';
import { FormEvent,useEffect,useState } from 'react'; import { api } from '../_lib/api';
type Org={id:string;name:string;type:string}; type Rel={id:string;relationshipType:string;status:string;healthScore:number;strategicScore:number;riskScore:number;sourceOrganization:{id:string;name:string};targetOrganization:{id:string;name:string};owner?:{name:string}};
export default function Page(){const [items,setItems]=useState<Rel[]>([]);const [orgs,setOrgs]=useState<Org[]>([]);const [source,setSource]=useState('');const [target,setTarget]=useState('');const [kind,setKind]=useState('STRATEGIC');const [error,setError]=useState('');
 async function load(){try{setItems(await api<Rel[]>('/relationships'));setOrgs(await api<Org[]>('/organizations'))}catch(e){setError((e as Error).message)}}useEffect(()=>{load()},[]);
 async function create(e:FormEvent){e.preventDefault();try{await api('/relationships',{method:'POST',body:JSON.stringify({sourceOrganizationId:source,targetOrganizationId:target,relationshipType:kind})});setSource('');setTarget('');await load()}catch(e){setError((e as Error).message)}}
 return <main className="shell"><header className="topbar"><strong>SRIP</strong><nav><a href="/">Dashboard</a><a href="/organizations">Organizations</a><a href="/people">People</a><a href="/intelligence">Intelligence</a></nav></header><section className="hero"><p className="eyebrow">Phase 7 · Relationship First</p><h1>Relationships</h1><p>Cross-organization relationships with ownership, health, strategic value and risk.</p></section><section className="grid2"><form className="card" onSubmit={create}><h2>ایجاد رابطه</h2><label>مبدأ<select value={source} onChange={e=>setSource(e.target.value)} required><option value="">انتخاب کنید</option>{orgs.map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select></label><label>مقصد<select value={target} onChange={e=>setTarget(e.target.value)} required><option value="">انتخاب کنید</option>{orgs.map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select></label><label>نوع رابطه<input value={kind} onChange={e=>setKind(e.target.value)} required/></label><button>ایجاد رابطه</button>{error&&<p className="error">{error}</p>}</form><section className="card"><h2>Relationship Directory</h2>{items.length===0?<p>رابطه‌ای ثبت نشده است.</p>:<div className="list">{items.map(r=><article className="listRow" key={r.id}><div><strong><a href={`/relationships/${r.id}`}>{r.sourceOrganization.name} ↔ {r.targetOrganization.name}</a></strong><small>{r.relationshipType} · {r.status}</small></div><span>Health {r.healthScore} · Strategic {r.strategicScore} · Risk {r.riskScore}</span></article>)}</div>}</section></section></main>}
