import React from'react';import{SafeAreaView,Text}from'react-native';import{styles}from'../lib/ui';export default function ConflictResolution(){return <SafeAreaView style={styles.content}><Text style={styles.title}>Conflict Resolution</Text><Text style={styles.card}>Conflicts are surfaced for explicit user choice; the client never silently overwrites server data.</Text></SafeAreaView>}
