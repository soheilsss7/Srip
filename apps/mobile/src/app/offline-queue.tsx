import React,{useEffect,useState}from'react';import{SafeAreaView,Text,Pressable}from'react-native';import{queuedMutations,clearQueue}from'../services/offline-queue';import{styles}from'../lib/ui';export default function OfflineQueue(){const[n,setN]=useState(0);const load=()=>queuedMutations().then(x=>setN(x.length));useEffect(()=>{load()},[]);return <SafeAreaView style={styles.content}><Text style={styles.title}>Offline Queue</Text><Text style={styles.card}>{n} queued mutation(s)</Text><Pressable style={styles.button} onPress={()=>clearQueue().then(load)}><Text style={styles.buttonText}>Clear local queue</Text></Pressable></SafeAreaView>}
