import React,{useState}from'react';import{SafeAreaView,Text,Pressable}from'react-native';import{useSession}from'../state/session';import{styles}from'../lib/ui';export default function Sync(){const{syncOffline}=useSession();const[r,setR]=useState<any>();return <SafeAreaView style={styles.content}><Text style={styles.title}>Sync</Text><Pressable style={styles.button} onPress={()=>syncOffline().then(setR)}><Text style={styles.buttonText}>Sync now</Text></Pressable>{r&&<Text style={styles.card}>Sent: {r.sent} · Remaining: {r.remaining}</Text>}</SafeAreaView>}
