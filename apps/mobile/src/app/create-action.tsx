import React,{useState}from'react';import{SafeAreaView,Text,TextInput,Pressable,ScrollView}from'react-native';import{useRouter}from'expo-router';import{apiPostOffline}from'../services/api-client';import{useSession}from'../state/session';import{styles}from'../lib/ui';
export default function CreateAction(){const{token}=useSession();const router=useRouter();const[t,setT]=useState('');const[e,setE]=useState('');async function save(){try{await apiPostOffline('/actions',{title:t},token);router.back()}catch(x){setE((x as Error).message)}}return <SafeAreaView style={styles.screen}><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>New Action</Text><TextInput style={styles.input} placeholder="Action title" value={t} onChangeText={setT}/>{e?<Text style={styles.error}>{e}</Text>:null}<Pressable style={styles.button} onPress={save}><Text style={styles.buttonText}>Save</Text></Pressable></ScrollView></SafeAreaView>}
