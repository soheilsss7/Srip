import React, { useState } from 'react';
import { Alert, SafeAreaView, Text, TextInput, Pressable, View } from 'react-native';
import { useSession } from '../state/session';
import { styles } from '../lib/ui';
export default function Login(){ const { signIn }=useSession(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [busy,setBusy]=useState(false); const submit=async()=>{setBusy(true);try{await signIn(email.trim(),password)}catch(e){Alert.alert('Sign in failed',e instanceof Error?e.message:'Unable to sign in')}finally{setBusy(false)}}; return <SafeAreaView style={styles.screen}><View style={styles.content}><Text style={styles.title}>SRIP</Text><Text style={styles.subtitle}>Strategic Relationship Intelligence Platform</Text><TextInput style={styles.input} autoCapitalize="none" keyboardType="email-address" placeholder="Email" value={email} onChangeText={setEmail}/><TextInput style={styles.input} secureTextEntry placeholder="Password" value={password} onChangeText={setPassword}/><Pressable style={styles.button} disabled={busy} onPress={submit}><Text style={styles.buttonText}>{busy?'Signing in…':'Sign in'}</Text></Pressable><Text style={styles.subtitle}>Authentication uses the existing API and stores the session token in device secure storage.</Text></View></SafeAreaView> }
