# Mobile features

Phase 16 establishes a domain-driven mobile shell backed by the existing API.

Implemented: secure session persistence, authenticated API client, tab navigation, home workspace, relationships, meetings, actions, notification list, detail routes, pull-to-refresh, loading/error/empty states, and sign-out.

The API remains the source of truth. The mobile client does not duplicate authorization logic.
